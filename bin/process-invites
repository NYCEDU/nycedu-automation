#!/usr/bin/env node

const inviteUsers = require('../invite-users'),
    followUsers = require('../follow-users'),
    getInvitees = require('../get-invitees')

getInvitees( users => {
    inviteUsers(users)
    followUsers(users)
})
